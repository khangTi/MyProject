<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/login"
    app:startDestination="@id/connectFragment">

    <fragment
        android:id="@+id/connectFragment"
        android:name="com.kt.myproject.ui.fragment.call.ConnectFragment"
        android:label="ConnectFragment"
        tools:layout="@layout/fragment_connect" />
    <action
        android:id="@+id/action_global_connectFragment"
        app:destination="@id/connectFragment" />

    <fragment
        android:id="@+id/callFragment"
        android:name="com.kt.myproject.ui.fragment.call.CallFragment"
        android:label="fragment_call"
        tools:layout="@layout/fragment_call" />
    <action
        android:id="@+id/action_global_callFragment"
        app:destination="@id/callFragment" />

</navigation>